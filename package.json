{
  "name": "contacts",
  "version": "1.0.0",
  "description": "Contact",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js",
    "heroku-postbuild": "cd client && yarn && yarn build"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "mongoose": "^5.10.9"
  },
  "engines": {
		"node": ">= 10.3.0",
		"npm": "^6.8.0"
	}
}
